21/09/2026

## react intro

    What is React?
  

  
    1) React is a JavaScript library (not a framework) developed by Facebook (Meta) for building user interfaces (UIs), especially Single Page Applications (SPAs).

    2) It helps developers build reusable UI components that update efficiently when data changes.

    Instead of reloading a whole webpage when something changes, React updates only the parts of the UI that need to change.

    -------
    Why do we need React?

	It makes UI development faster, cleaner, and more efficient.

	Provides a better user experience with smooth updates without page reloads.


    ## must need to install node.js
 
## procedure to initiate project

    step 1 : npm create vite@latest [project_name]
    step 2 : select react
    step 3 : select javascript+ react compailer
    step 4 : npm install                // install node module packages
    step 5 : npm run dev 

    if you want to re-run your project mean 
        npm run dev

22/09/2026

## Props

    Props is nothing but drilling data from parent component to child component.

        Declearing Props
        Destructuring Props

## Conditional rendering

    Conditional rendering means showing or hiding part of UI basde onsome condition.

            ternary operator  {condition ? true : false}
            logical operator   && || !


23/09/2026

## List rendering in react

    rendering multiple items dynamically using .map()

    arr=[1,2,3,4]

    arr.map((num)=>{
        num*2
    })

    arr=[2,4,6,8]


## styleing in react

    global styleing -- index.css and app.css is called as global styleing.
    local styleing -- to create a seperate css file for a component.
    module styleing -- to create a seperate css file for a component .
                        file name in the formate of [fileName.module.css]
    inline styleing -- to implement inline style we need to declear [style] attribute in {{padding:"20px"}}
    object styleing -- we need to declear a styleing property in object with key & value pair


24/09/2026  & 25/09/2026

## usestate() 

    usestate is a react hook that allows you to add state (data that can change over time)
    to a functional component.

    Normally in js function , variables disappear after the function runs.
    But with usestate, react remembers values b/w re-render.

    syntax:

    const[state,setState]=usestate(initiateValue)

    state-- the current value of the state variables
    setState-- a function that update the state
    initiateValue -- the starting value of the state.
    

## useEffect()

    In react, useEffect is a hook that lets you perform side effect in functional component.
    Side effect are tasks that happen outside the normal react rendeering process,like:

            fetch the data from an API
            Updating the browser dom manually
            Starting.stoping timers


    syntax:

            useEffect(()=>{
                //code
            },[dependency 1, dependency 2,,....])

            [] -- it will execut it once at begining.


## domain

    a domin is the main website address people use to access a website.

    ex: example.com

## sub domin

     a subDomain is  acreated before the main domain to seperate a particular Applications,
     server, or sector

     ex:
            example.com
            admin.example.com
            blog.example.com

## types of domain

.com -- commercial
.in -- india
.org --- organization
.net -- network
.edu -- education
.gov -- government
.mil -- military
.co -- company


